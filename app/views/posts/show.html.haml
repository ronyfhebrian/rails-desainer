= image_tag @post.image.url(:medium)
%h1= @post.title
%p= @post.url
%p= @post.description
%p= @post.user.name

= link_to "beranda", root_path
= link_to "Edit", edit_post_path(@post)
= link_to "Hapus", post_path(@post), method: :delete, data: { confirm: "Apakah Kamu yakin? "}
